# 작업하는 방법

[README](README.md)의 "작업 규칙"을 자세히 풀어쓴 문서입니다. PR을 올리기 전에 한 번 읽으면 됩니다.

---

## 한 사이클

```
git switch main
git pull                                작업 시작 전에 항상 최신으로 맞춥니다
git switch -c ai/detection-baseline     <파트>/<하는일>
... 작업 ...
uv run pytest                           올리기 전에 로컬에서 돌려봅니다
git push -u origin ai/detection-baseline
```

그다음 GitHub에서 PR을 올립니다.

**`main`에 직접 push하지 않는 것은 규칙이고, 장치로 막아두지 않았습니다.** `git push origin main` 이 그냥 됩니다. 그래서 사고가 나면 되돌리는 것도 사람 손입니다 — 첫 줄의 `git switch -c` 를 습관으로 만드는 게 유일한 방어입니다.

## 브랜치 이름

```
ai/...      AI파트
sw/...      SW파트
data/...    데이터파트
```

브랜치 목록만 보고 누가 무엇을 하는지 알 수 있게 하려는 것입니다. 그래서 `test`, `fix`, `내작업` 같은 이름은 쓰지 않습니다.

## 커밋 메시지

`[파트] 무엇을 했다` 형식입니다.

```
[AI] 탐지 모델 학습 스크립트 추가
[SW] 디코딩 재시도 루프 구현
[데이터] 곡률 왜곡 합성 함수 추가
[문서] 탐지 모델 선정 결과 기록
```

영어 타입 키워드(`feat:`, `fix:`)는 쓰지 않습니다. 외울 게 하나 늘어나는 만큼의 이익이 없습니다.

## 리뷰어 지정

**PR을 올린 사람이 직접 지정합니다.** 오른쪽 `Reviewers`에서 같은 파트원 1명을 고르세요. 누가 어느 파트인지는 [README의 표](README.md#내-파트는-어디서-시작하나요)에 있습니다.

승인을 받고 머지하세요. **승인 없이도 머지 버튼은 눌립니다** — 강제하지 않습니다. 리뷰어도 자동으로 지정되지 않으니 빼먹으면 아무도 모르는 채로 PR이 방치됩니다.

`wemeet/schemas.py`를 바꿨다면 **AI·SW 양쪽에서 한 명씩** 지정하세요. 이유는 아래에 있습니다.

## 상의하고 바꿔야 하는 파일

| 파일 | 왜 |
|---|---|
| `wemeet/schemas.py` | AI파트와 SW파트가 주고받는 데이터 형식입니다. 한쪽이 말없이 바꾸면 반대쪽이 전부 깨집니다 |
| `pyproject.toml` | 의존성 목록, import 규칙, 코드 스타일 설정이 들어 있습니다 |
| `.github/` | CI 검사 항목입니다. 여기가 느슨해지면 위의 규칙들이 강제되지 않습니다 |

강제하는 장치는 없습니다. 규칙으로만 남아 있으니 지켜주세요.

## CI에서 빨간 X가 떴을 때

**빨간 X여도 머지 버튼은 눌립니다.** 막아두지 않았으니, 초록으로 만든 다음 머지하는 것은 올린 사람 책임입니다. 빨간 채로 머지하면 다음 사람이 자기가 뭘 깨뜨렸는지 모르는 상태로 시작합니다.

PR마다 검사 3개가 자동으로 돌아갑니다.

| 검사 | 실패했을 때 |
|---|---|
| `ruff` | `uv run ruff format .` 을 돌리면 대부분 자동으로 고쳐집니다. 남은 것은 `uv run ruff check .` 이 알려줍니다 |
| `import-linter` | 파트 간 import 규칙을 어겼습니다. AI파트는 `wemeet.sw`를 import하지 않습니다. 필요해 보이면 설계가 잘못된 것이니 상의하세요 |
| `pytest` | 테스트가 깨졌습니다. `uv run pytest -x` 로 첫 실패만 보면서 고치는 게 빠릅니다 |

로그는 PR 아래 `Details`를 누르면 나옵니다. **빨간 X는 혼내는 게 아닙니다.** 뭐가 잘못됐는지 5분 안에 알려주는 장치입니다.

## 머지 후

브랜치는 GitHub에서 `Delete branch` 버튼으로 지웁니다. 로컬은 이렇게 정리합니다.

```bash
git switch main
git pull
git branch -d ai/detection-baseline
```

## 리뷰할 때

- 스타일 지적은 하지 않습니다. `ruff`가 이미 봤습니다. 로직만 봅니다
- 모르면 "이거 왜 이렇게 했어요?"라고 물어보세요. 질문은 지적이 아닙니다
- 하루 안에 봐주세요. 리뷰가 밀리면 PR이 쌓이고 충돌이 늘어납니다
