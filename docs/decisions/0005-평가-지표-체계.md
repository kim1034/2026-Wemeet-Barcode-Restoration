# 0005. 평가 지표 체계

- **상태**: 미결정
- **기한**: 9월 2주차
- **담당**: 팀장 + 전원 (멘토·지도교수 확인 필요)
- **결정일**:

---

## 무엇을 정하는가

기획서 4.6의 평가 지표 체계를 확정한다. 검토에서 **문제 3개**가 발견되어 그대로 쓸 수 없는 상태다.

**이 문서는 기획서를 수정하는 내용이므로 멘토와 지도교수 확인이 필요하다.** 팀 내부 결정만으로 진행하지 않는다.

---

## 문제 1. SSIM은 실촬영 데이터에서 측정할 수 없다

이건 의견 문제가 아니라 **물리적 제약**이다.

- 기획서 4.6.1: SSIM을 "복원 이미지와 원본(GT) 간 구조적 일치도"로 정의
  → **2026-08-04부터 SSIM은 주지표가 아니다.** 이미지를 생성하지 않으므로 이미지 품질 지표가 의미를 잃었다. 주지표는 보정 후 디코딩 성공률이다 (`0003` 참고)
- 기획서 4.6.2: "합성 데이터가 아닌 실촬영 데이터**만으로** 최종 평가"

그런데 **구겨진 비닐 사진에 대응하는 픽셀 정렬된 평탄한 정답 이미지가 물리적으로 존재하지 않는다.** 같은 송장을 펴서 다시 촬영해도 픽셀이 정렬되지 않는다.

→ SSIM은 **합성 검증셋 전용**이다. 실촬영셋에서 측정 가능한 것은 디코딩 성공률과 지연시간뿐이다.

### 제안: 지표 표에 "측정 대상" 열을 추가한다

| 지표 | 측정 대상 | 목표 |
|---|---|---|
| mAP@0.5, mAP@0.5:0.95, 각도 오차(°) | 합성 검증셋 | 아래에서 확정 |
| SSIM (주), PSNR (보조) | **합성 검증셋 전용** | SSIM 0.85 |
| 종합 디코딩률 | 실촬영 `eval` | 아래에서 확정 |
| 난인식 구제율 | 실촬영 `eval`의 난인식 부분집합 | 아래에서 확정 |
| **오독률 (false decode)** | 실촬영 `eval` | **0%** |
| E2E 지연 (p50 / p95) | 실촬영 `eval` | 500ms |

---

## 문제 2. 최종 KPI의 분모 정의가 베이스라인 비교를 무의미하게 만든다

기획서 4.6.1은 최종 KPI를 **"pyzbar 단독 실패 샘플 대상, 파이프라인 통과 후 성공률 90% 이상"**으로 정의한다.

이 정의면 4.6.2의 3조건 비교에서 **조건(1) pyzbar 단독의 성공률이 정의상 0%**가 되어 비교가 성립하지 않는다.

또한 "실패한 것 중 90%를 살린다"는 상당히 야심찬 수치다. 참고로 [Nature Sci Rep 2025 논문](https://www.nature.com/articles/s41598-025-29720-w)의 부분 가림 조건 디코딩 정확도가 78.3%다.

### 제안: 지표를 셋으로 분리한다

| 지표 | 정의 | 의미 |
|---|---|---|
| **종합 디코딩률** | 전체 테스트셋 중 정답 일치 비율 | 시스템 전체 성능 |
| **난인식 구제율** | 디코더 단독 실패 샘플 중 파이프라인이 살린 비율 | AI의 기여도 |
| **오독률** | 틀린 번호를 출력한 비율 | **0%여야 한다** |

오독률을 별도로 두는 이유: 물류에서는 **틀린 번호를 자신 있게 내놓는 것이 못 읽는 것보다 나쁘다.** 잘못된 화물 정보가 그대로 흘러간다. 기획서 4.6.2의 "완전 일치 판정" 원칙을 지표로 승격한 것이다.

### 현장 언어로는

실무에서 의미 있는 표현은 이쪽이다.

> **노리드율 N% → M% 이하**

기획서에는 이 지표가 없다. 추가를 검토한다.

---

## 문제 3. mAP 0.95 목표의 근거와 지표 선택

기획서는 mAP 0.95 이상을 목표로 하지만 근거가 제시되지 않았다. 위 논문이 창고 환경 YOLOv8로 **mAP@0.5 92.4%**, mAP@0.5:0.95 64.5%다. 952장 + 자체 라벨로 0.95는 낙관적이다.

또한 **mAP@0.5만으로는 불충분하다.** OBB에서는 각도 오차가 크롭 품질을 직접 좌우하는데, IoU 0.5 기준은 각도가 어긋나도 통과한다. 크롭이 비뚤면 디코더가 못 읽는다.

### 제안

- **mAP@0.5:0.95**와 **각도 오차(°)**를 함께 사용
- 구체적 목표 수치는 `0004`의 데이터 실태 파악 후 확정

---

## 목표 수치 확정 (베이스라인 측정 후)

**기준선 없이는 목표를 정할 수 없다.** `0006`의 디코더 베이스라인 측정 결과를 받아 채운다.

> **2026-08-28 추가 — 분모를 잘못 잡으면 Stage 2 의 기여가 지표에서 사라진다.**
>
> [합성 스파이크](../experiments/2026-08-28-synthesis-spike/README.md)에서
> **조기 종료(1차 디코딩 성공) 비율이 예상보다 훨씬 높게** 나왔다 — 합성 기준
> 모듈 폭 2.8~4.0px 에서 75%, 1.6~2.2px 에서 48%. 모듈이 5.5px 이면 원통 70°까지
> 보정 없이 읽힌다.
>
> 그러면 전체 데이터에 대한 "종합 디코딩률"로 기하 추정을 평가하면,
> 대부분이 1차에서 끝나 **모델을 바꿔도 숫자가 거의 안 움직인다.** 지표를 이렇게 나눈다.
>
> | 지표 | 분모 | 무엇을 말하나 |
> |---|---|---|
> | 종합 디코딩률 | 전체 | 시스템 성능. 현장에 보고할 숫자 |
> | **조기 종료율** | 전체 | 이 값이 높으면 Stage 2 의 기여 여지가 작다. **꼭 같이 보고할 것** |
> | **난인식 구제율** | **1차 디코딩 실패분만** | **Stage 2 가 실제로 한 일.** 모델 비교의 주지표 |
>
> 그리고 `0003` 의 모델 비교용 평가 세트는 **목표 구간(1차 실패 & 보정 성공)에
> 집중시켜야** 후보 간 차이가 드러난다. 전체 분포로 평가하면 모델 A·B 가 둘 다
> 80% 대로 나와 구분이 안 된다.

| 지표 | 베이스라인 (현재) | 목표 | 근거 |
|---|---|---|---|
| 종합 디코딩률 | | | |
| 난인식 구제율 | | | |
| 오독률 | | 0% | |
| mAP@0.5:0.95 | | | |
| 각도 오차 | | 5° 이하 | 크롭 품질 |
| SSIM | | 0.85 | 기획서 |
| E2E p95 | | 500ms | 기획서 |

---

## 훼손 유형별 세분화

기획서 4.6.2대로 테스트셋을 세 유형으로 나눠 각각 산출한다. 강점과 취약 구간을 식별하기 위한 것이다.

| 유형 | 성공률 |
|---|---|
| 곡률 변형 | |
| 빛 반사 | |
| 복합 훼손 | |

그리고 시스템의 **정상 작동 보장 범위**를 명시적으로 문서화한다. 기획서 4.2.3의 "픽셀 손실률 40% 이하" 기준을 실측으로 검증·조정한다.

---

## 피드백 루프 기여도

재시도 횟수별 추가 성공률을 측정해 상한 3회의 타당성과 지연 대비 효용을 분석한다 (기획서 4.6.2).

| 시도 | 누적 성공률 | 추가 성공 | 추가 지연 |
|---|---|---|---|
| 0차 | | — | — |
| 1차 재시도 | | | |
| 2차 재시도 | | | |
| 3차 재시도 | | | |

결과에 따라 `0006`의 재시도 순서를 조정한다.

---

## 멘토에게 확인할 것

1. 문제 1 — SSIM을 합성 검증셋 전용으로 분리하는 것이 타당한가
2. 문제 2 — 최종 KPI를 셋으로 분리하는 것이 사업단 평가 기준과 맞는가
3. 문제 3 — mAP 목표를 베이스라인 측정 후 확정하는 것이 허용되는가
4. 노리드율 감소를 지표에 추가할 것인가

## 결정과 이유

미결정

## 영향받는 것

- 기획서 4.6 본문
- `decisions/0002`, `0003` — 목표 수치
- 11월 성능 평가 보고서 구조
